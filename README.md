# AIfor8x

Simple local project for AI tools and visualizer components.

## Structure
- `app.js`, `index.html`, `index.css`
- `components/` — analyzer, chat, visualizer
- `ai-logs/` — conversation logs

## Usage
Run from a static server or open `index.html` in a browser.
